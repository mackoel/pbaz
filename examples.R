y <- get_gt_snp(conn, ofl, gt_like = '%Ortan%', gt_limit = 20, snp_like = '%C_T%', snp_limit = 30)

ofl <- "test.csv"

y <- get_gt_cnum(conn, ofl, gt_like = '%Bari%', gt_limit = 20, cn_like = '%Ca1%', cn_limit = 30)
